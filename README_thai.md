
---

# 🚀 hyper_pipe V3

> ระบบส่งไฟล์ผ่าน HTTP แบบ **ไม่ต้องเชื่อ server**
> (Zero-trust file distribution over HTTP)

`hyper_pipe` คือเครื่องมือดาวน์โหลดไฟล์ที่ออกแบบมาให้:

* 🔐 ตรวจสอบความถูกต้องของข้อมูลทุกส่วน
* 🧩 แบ่งไฟล์เป็น chunk อัตโนมัติ
* ✍️ ใช้ลายเซ็นดิจิทัลเพื่อยืนยันความแท้ของไฟล์
* 🌐 ทำงานบน HTTP ธรรมดา (ผ่าน Range request)

แม้ว่า:

* server จะโกงไฟล์
* network จะมีปัญหา
* ไฟล์จะถูกแก้ไขระหว่างทาง

→ client จะตรวจจับได้ทันที

---

## ✨ คุณสมบัติ (Features)

* 🔹 แบ่งไฟล์แบบ content-defined chunking
* 🔹 ตรวจ hash ของแต่ละ chunk (BLAKE3)
* 🔹 ไฟล์ index มีลายเซ็นดิจิทัล (Ed25519)
* 🔹 ไม่ต้องเชื่อ server (zero-trust)
* 🔹 ใช้ HTTP ปกติ (Range request)
* 🔹 ตรวจจับไฟล์ปลอม / ไฟล์เสียได้ทันที

---

## 🧠 แนวคิดการออกแบบ

hyper_pipe ถูกออกแบบคล้ายกับระบบระดับโลก เช่น:

* BitTorrent (piece hash)
* IPFS (content addressing)
* ระบบอัปเดต OS แบบปลอดภัย (OTA)

หลักการ:

> ใช้ transport ที่ไม่ปลอดภัย (HTTP)
> แต่สร้างความปลอดภัยที่ระดับข้อมูล (data-level security)

---

## 🏗 โครงสร้างการทำงาน (Architecture)

```
      ฝั่งผู้สร้างไฟล์                    ฝั่งผู้ดาวน์โหลด
  ──────────────────────             ──────────────────────
      file.bin                          file.bin.idx
          │                                  │
          ▼                                  ▼
    แบ่งเป็น chunk                   ตรวจลายเซ็นไฟล์ index
          │                                  │
          ▼                                  ▼
    hash แต่ละ chunk                  ดาวน์โหลดด้วย Range
          │                                  │
          ▼                                  ▼
   สร้างไฟล์ .idx + sign            ตรวจ hash แต่ละ chunk
          │                                  │
          ▼                                  ▼
                                      รวมกลับเป็น file.bin
```

---

## 🔐 โมเดลความปลอดภัย (Security Model)

สมมติว่า:

* ❌ server อาจไม่ปลอดภัย
* ❌ network อาจถูกดักฟังหรือแก้ไข
* ❌ ไฟล์อาจถูกปลอมแปลง

สิ่งที่เชื่อถือ:

* ✅ ไฟล์ index (.idx) ที่มีลายเซ็น
* ✅ public key
* ✅ hash แบบ cryptographic (BLAKE3)

ถ้า:

* chunk ใดไม่ตรง hash
* index ไม่ผ่านการ verify

→ การดาวน์โหลดจะถูกยกเลิกทันที

---

## ⚠️ ข้อกำหนดของ HTTP Server

server ต้องรองรับ **HTTP Range request**

รองรับ:

* nginx
* apache
* busybox httpd
* caddy

ไม่รองรับ:

* `python -m http.server`

ทดสอบด้วย:

```bash
curl -I -H "Range: bytes=0-1023" http://host/file
```

ต้องได้:

```
HTTP/1.1 206 Partial Content
```

---

## 📦 การ build

```bash
cargo build --release
```

ไฟล์ที่ได้:

```bash
./target/release/hyper_pipe_v3
```

---

## 🔑 วิธีใช้งาน

### 1️⃣ สร้าง keypair

```bash
./hyper_pipe_v3 keygen
```

จะได้:

```
private.key
public.key
```

---

### 2️⃣ สร้าง index ของไฟล์

```bash
./hyper_pipe_v3 index test.bin private.key
```

จะได้:

```
test.bin.idx
```

---

### 3️⃣ เปิด HTTP server (ตัวอย่าง)

```bash
busybox httpd -f -p 8000
```

---

### 4️⃣ ดาวน์โหลดแบบปลอดภัย

```bash
./hyper_pipe_v3 download \
  http://127.0.0.1:8000/test.bin \
  test.bin.idx \
  public.key \
  ./out
```

ไฟล์จะอยู่ที่:

```
./out/test.bin
```

---

## 🧪 ตรวจสอบความถูกต้องของไฟล์

```bash
sha256sum test.bin out/test.bin
```

ค่าที่ได้ต้องเหมือนกัน

---

## 📁 โครงสร้างโปรเจกต์

```
src/
 ├── main.rs
 ├── cli.rs
 ├── chunker.rs
 ├── crypto.rs
 ├── index.rs
 ├── downloader.rs
 └── types.rs
```

---

## 🛣 แผนพัฒนาในอนาคต (Roadmap)

* [ ] ดาวน์โหลดแบบหลาย thread
* [ ] resume download
* [ ] progress bar
* [ ] บีบอัดข้อมูลด้วย zstd
* [ ] เข้ารหัสข้อมูลแต่ละ chunk
* [ ] multi-mirror download
* [ ] server mode
* [ ] P2P mode

---

## 📜 License

MIT

---

## 👨‍💻 ผู้พัฒนา

พัฒนาโดย: **traphumi**
hyper_pipe V3 — 2026

---

## 🎯 สรุป

hyper_pipe V3 พิสูจน์ว่า:

> สามารถสร้างระบบส่งไฟล์ที่ปลอดภัย
> บนโปรโตคอลที่ไม่ปลอดภัย (HTTP) ได้

โดยใช้:

* cryptography
* hash
* digital signature
* zero-trust model

---
